#include <stdio.h>
int main()
{
    int i=100,s;
    for (i=100;i<=200;i++)
    {
        if (i % 7 == 0)
        {
            s = s + i ;
        }
    }
    printf("the sum is: %d",s);
    return 0=
}